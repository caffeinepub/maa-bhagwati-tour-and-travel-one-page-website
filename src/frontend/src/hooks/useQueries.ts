// This file is intentionally minimal as the website is static and doesn't require backend queries
// All content is hardcoded in the components as per the requirements

export function useQueries() {
  // No backend queries needed for this static marketing website
  return {};
}
